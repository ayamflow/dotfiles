#!/usr/bin/env zsh

# Finder aliases
alias dl="cd ~/Downloads"
alias dt="cd ~/Desktop"
alias wk="cd ~/Travail"

# Git
alias ga = "git add -A"
alias gs = "git status"

# Virtual environments (Python)
alias venv="source ~/.virtualenvs/default/bin/activate;"